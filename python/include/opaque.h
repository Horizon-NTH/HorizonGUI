#pragma once

#include "Include.h"

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<int, 1>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<unsigned, 1>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<float, 1>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<double, 1>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<int, 2>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<unsigned, 2>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<float, 2>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<double, 2>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<int, 3>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<unsigned, 3>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<float, 3>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<double, 3>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<int, 4>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<unsigned, 4>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<float, 4>);

PYBIND11_MAKE_OPAQUE(hgui::kernel::Vector<double, 4>);
